# ─────────────────────────────────────────────────────────────────────────────
# Expense Tracker — Environment Variables
# Copy this file to .env and fill in the values before running docker compose.
#   cp .env.example .env
# ─────────────────────────────────────────────────────────────────────────────

# ── Clerk Authentication ──────────────────────────────────────────────────────
# Get this from https://dashboard.clerk.com → API Keys → Publishable Key
EXPO_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_REPLACE_ME

# ── CouchDB / PouchDB Sync ────────────────────────────────────────────────────
# URL the Expo app uses to reach CouchDB.
# When running on a physical device, replace "couchdb" with your host machine's
# LAN IP so the device can reach the container (e.g. http://admin:password@192.168.1.x:5984)
EXPO_PUBLIC_COUCHDB_URL=http://admin:password@couchdb:5984

# ── CouchDB Credentials ───────────────────────────────────────────────────────
# These seed the CouchDB container admin account.
COUCHDB_USER=admin
COUCHDB_PASSWORD=password

# ── Metro / Expo Host ─────────────────────────────────────────────────────────
# The LAN IP address Metro advertises to Expo Go clients.
#
# ► iOS / Android physical device (same Wi-Fi):
#     Set this to your host machine's LAN IP, e.g. 192.168.1.50
#     Then open Expo Go on the device and scan the QR code.
#     No Xcode required for iOS — Expo Go handles everything.
#
# ► Tunnel mode (cross-network):
#     Uncomment the tunnel command in docker-compose.yml metro service.
#     METRO_HOST is ignored in tunnel mode.
#
# ► Emulator only (Android Studio / Xcode Simulator on the same machine):
#     Leave as 0.0.0.0
METRO_HOST=0.0.0.0

# ── Expo Tunnel mode ─────────────────────────────────────────────────────────
# Set to true to use ngrok tunnel (Expo Go on any network, no LAN IP needed).
# When false (default) Metro uses LAN mode — requires same Wi-Fi as the device.
EXPO_TUNNEL=false

# ── EAS / Expo Build Token ────────────────────────────────────────────────────
# Required only for EAS Build (cloud builds) or tunnel mode. Leave blank otherwise.
EXPO_TOKEN=
